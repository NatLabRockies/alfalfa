# Convenience overlay that combines development mode (live-reload web/worker)
# with the optional historian stack (InfluxDB + Grafana), so you don't need
# to chain three `-f` flags by hand.
#
# This intentionally duplicates the union of docker-compose.dev.yml and
# docker-compose.historian.yml (Compose's `include:` directive does not
# reliably deep-merge multiple override files here — it was found to drop
# the worker/web `target: dev` build override). If you change
# docker-compose.dev.yml or docker-compose.historian.yml, update this file
# to match.
#
# Usage:
#   HISTORIAN_ENABLE=true docker compose -f docker-compose.yml -f docker-compose.dev.historian.yml up --build --scale worker=2

services:
  web: # main haystack application
    container_name: alfalfa_web
    build:
      # the purpose of context one level above dockerfile
      # is to utilize shared code, specifically in db
      dockerfile: alfalfa_web/Dockerfile
      context: .
      # The dev instance still needs works, so default to base for now
      target: dev
    environment:
      - NODE_ENV=development
      - HISTORIAN_ENABLE=true
      - GRAFANA_URL_EXTERNAL
    volumes:
      - ./alfalfa_web:/srv/alfalfa
      # create volumes for the build and node_modules so that
      # the resources are not copied back to the local host when
      # mounting.
      - /srv/alfalfa/build
      - /srv/alfalfa/node_modules
  worker:
    build:
      # the purpose of context one level above is to install the entire alfalfa package
      # and to copy the wait-for-it.sh and start_worker.sh files in the deploy directory
      dockerfile: alfalfa_worker/Dockerfile
      context: .
      target: dev
    environment:
      - HISTORIAN_ENABLE=true
    volumes:
      - ./alfalfa_worker:/alfalfa/alfalfa_worker
      - ./tests:/alfalfa/tests
  influxdb:
    container_name: alfalfa_influxdb
    image: influxdb:1.8
    hostname: ${INFLUXDB_HOST}
    ports:
      - "8086:8086"
    environment:
      - INFLUXDB_DB
      - INFLUXDB_HTTP_AUTH_ENABLED=${INFLUXDB_HTTP_AUTH_ENABLED}
      - INFLUXDB_ADMIN_USER=${INFLUXDB_USERNAME}
      - INFLUXDB_ADMIN_PASSWORD=${INFLUXDB_PASSWORD}
    restart: always
  grafana:
    container_name: alfalfa_grafana
    build:
      context: "./grafana"
      args:
        - INFLUXDB_ADMIN_USER=${INFLUXDB_USERNAME}
        - INFLUXDB_ADMIN_PASSWORD=${INFLUXDB_PASSWORD}
        - INFLUXDB_DB
        - INFLUXDB_HOST
    hostname: grafana
    image: ${REGISTRY_BASE_URI}/grafana:${VERSION_TAG}
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER
      - GF_SECURITY_ADMIN_PASSWORD
      - INFLUXDB_DB
      - INFLUXDB_HOST
      - INFLUXDB_ADMIN_USER=${INFLUXDB_USERNAME}
      - INFLUXDB_ADMIN_PASSWORD=${INFLUXDB_PASSWORD}
    depends_on:
      - ${INFLUXDB_HOST}
